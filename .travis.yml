language: java
branches:
  only:
    - Feature/ForecastTrigger

notifications:
   email:
     - akmehra@iu.edu
     - pmpande@iu.edu

jdk:
  - oraclejdk8
sudo: false
install: true
script: cd "StormCheck" && mvn clean install -DskipTests=true

before_deploy:
  - cd ..
  - zip -r TeamFlash_ForecastTrigger.zip StormCheck || true
  - mkdir -p "ForecastTrigger"
  - mv TeamFlash_ForecastTrigger.zip ForecastTrigger/ForecastTrigger.zip || true

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamflash1
    local_dir: ForecastTrigger
    region: us-west-2
    skip_cleanup: true
    acl: public_read
    detect_encoding: true
    on:
      branch: Feature/ForecastTrigger


  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamflash1
    key: TeamFlash_ForecastTrigger.zip
    bundle_type: zip
    application: TeamFlash1
    deployment_group: TeamFlash1
    region: us-west-2
    on:
      branch: Feature/ForecastTrigger