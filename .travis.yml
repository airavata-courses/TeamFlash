language: java
branches:
  only:
    - Feature/Registry

notifications:
   email:
     - akmehra@iu.edu
     - pmpande@iu.edu

jdk:
  - oraclejdk8
sudo: false
install: true
script: cd "Registry" && mvn clean install -DskipTests=true

before_deploy:
  - cd ..
  - zip -r TeamFlash_Registry.zip Registry scripts appspec.yml || true
  - mkdir -p "registry"
  - mv TeamFlash_Registry.zip registry/Registry.zip || true

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamflashwest
    local_dir: registry
    region: us-west-1
    skip_cleanup: true
    acl: public_read
    detect_encoding: true
    on:
      branch: Feature/Registry


  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamflashwest
    key: Registry.zip
    bundle_type: zip
    application: TeamFlash
    deployment_group: TeamFlash
    region: us-west-1
    on:
      branch: Feature/Registry
