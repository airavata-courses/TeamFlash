language: python
python:
  - "2.6"
  - "2.7"
  - "pypy"

before_script:
  - sudo apt-get install pychecker

branches:
  only:
    - MileStone1
    - master
    - dev

notifications:
  email:
    - akmehra@iu.edu
    - pmpande@iu.edu
    - girish-gabra@iu.edu
    - debasisdwivedy@iu.edu

sudo: true
 
install:
  - pip install pybuilder
  - pyb install_dependencies

script: cd "StormClustering" && "pyb"
